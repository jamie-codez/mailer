# GitHub Packages Configuration
@blacdot:registry=https://npm.pkg.github.com/
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}

# npm Configuration
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# General npm settings
save-exact=true
package-lock=false

# GitHub Packages requires scoped packages
@blacdot:registry=https://npm.pkg.github.com/
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}

# Always include optional dependencies
optional=false

# Disable package-lock.json generation
package-lock=false

# Set the default registry (can be overridden by publishConfig in package.json)
registry=https://registry.npmjs.org/

# Ensure auth token is used
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
# Set the access level for scoped packages
@blacdot:access=public